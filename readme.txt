STEPS TO WORK THE CODE
Run->cmd (command prompt)->File path->python main.py –image “image name with extension”.
To install the tesseract Engine, use the command “pip install pytesseract”.
Tesseract Engine (Must need to be installed).
Open computer version can be installed by using the command ”pip install opencv-python”.
we need to install the necessary packages to run the code. To install the package, use the code “pip install package”.


